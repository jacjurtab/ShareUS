<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.misviajes.tabs.ViajesPasajeroFragment">

    <ListView
        android:id="@+id/listado"
        android:layout_width="fill_parent"
        android:layout_height="match_parent"
        android:divider="@null"
        android:layout_marginHorizontal="10dp">
    </ListView>

    <include layout="@layout/empty" android:id="@+id/empty" android:visibility="invisible"/>
    <include layout="@layout/loading" android:id="@+id/loading" android:visibility="visible"/>

</FrameLayout>