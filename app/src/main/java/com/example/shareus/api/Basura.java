package com.example.shareus.api;

import com.android.volley.RequestQueue;
import com.example.shareus.model.Viaje;
import java.lang.Math;

import org.json.JSONException;

import java.util.List;

public interface Basura {

   // public  boolean crearViaje(int conductor, int origen, int destino, Long fecha_hora, int max_plazasRequestQueue mRequestQueue);
    //public static List<Viaje> obtenerViajesDisponibles(boolean disponible,RequestQueue mRequestQueue);
    //public Viaje obtenerViaje(int idViaje, RequestQueue mRequestQueue) throws JSONException;
   // public static List<Viaje> obtenerViajesFuturos_cond(int idViaje, boolean vencidos,RequestQueue mRequestQueue);
    //public static List<Viaje> obtenerViajesFuturos_pas(int idViaje, boolean vencidos, RequestQueue mRequestQueue);
   // public static List<Viaje> obtenerViajesPasados_user(int idViaje, boolean vencidos, RequestQueue mRequestQueue);  //pasdos cod + pasados user
    //public static boolean eliminarViaje(int viaje, RequestQueue mRequestQueue);
    //public static boolean insertarPasajero(int viaje, int pasajero, RequestQueue mRequestQueue);
    //public static boolean eliminar_pasajero(int viaje, int pasajero, RequestQueue mRequestQueue);


}
